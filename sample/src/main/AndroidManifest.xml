<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.alvin.mvvm_framework">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SampleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MVVMFramework">
        <!-- 避免横竖屏切换重新创建Activity android:configChanges="orientation|keyboardHidden|screenSize|uiMode" -->
        <activity
            android:name=".ui.activity.SampleStatusLayoutActivity"
            android:configChanges="orientation|keyboardHidden|screenSize|uiMode" />
        <activity
            android:name=".LaunchActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.SampleMainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize|uiMode"
            android:launchMode="singleTask" />
    </application>

</manifest>